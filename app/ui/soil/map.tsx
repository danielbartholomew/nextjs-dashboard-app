export default function Map() {
  return (
    // LATER THIS WILL BE A MAP
    // FOR NOW JUST RENDER A SOLID GREEN BOX
    // The parent should make this element stretch (parent must be display:flex and allow growth).
    <div className="w-full h-full bg-green-500"></div>

  );
}